version: "3.9"

services:
  backend:
    container_name: nutrition_be
    image: aliadnan107/e-nutrition-be:latest # Use the image from Docker Hub
    env_file:
      - .env
    environment:
      NODE_ENV: production
      DB_HOST: postgres
    depends_on:
      - postgres
    ports:
      - "3000:3000"
    networks:
      - app-network
    restart: unless-stopped

  postgres:
    image: postgres:14-alpine
    container_name: asd_db
    restart: always
    environment:
      POSTGRES_DB: ${DB_NAME}
      POSTGRES_USER: ${DB_USERNAME}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    ports:
      - "5433:5432"
    volumes:
      - asd-postgres:/var/lib/postgresql/data
    networks:
      - app-network

volumes:
  asd-postgres:

networks:
  app-network:
    driver: bridge
